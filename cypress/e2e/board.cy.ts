describe("Board", () => {
  beforeEach(() => {
    cy.login();
    cy.resetData();
  });

  it("creates a new board", () => {
    // Use Cypress Studio to record actions here
    // Click "Add commands to test" in the test runner
  });
});
